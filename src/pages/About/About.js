import React, { useContext } from 'react';
import AuthNavbar from '../../components/AuthNavbar';

const About = () => {
  return (
    <div>
      <AuthNavbar />
      <p>Sobre Nós</p>
      </div>
  );
};

export default About;
